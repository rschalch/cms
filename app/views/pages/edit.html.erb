<%= link_to('Back to Index', {action: :index, subject_id: @subject.id}) %>

<div>
  <h2>Edit Page</h2>

  <%= form_for :page, :url => {action: :update, subject_id: @subject.id, id: params[:id]} do |f| %>
		<%= render(partial: 'form', locals: {:f => f} ) %>
    <div>
      <%= submit_tag 'Update Page' %>
    </div>
  <% end %>
</div>